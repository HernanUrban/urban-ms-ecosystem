spring:
  profiles:
    active: dev
  application:
    name: discovery-service
  cloud:
    config:
      profile: dev
      enabled: false
      uri: http://localhost:8888
      username: admin-urban
      password: s3cr3t
