spring:
  profiles:
    active: dev
  application:
    name: discovery-service
  cloud:
    config:
      profile: dev
      enabled: true
      uri: ${CONFIG_URI:http://localhost:8888}
      username: admin-urban
      password: s3cr3t
